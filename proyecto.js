const taskForm=document.getElementById('taskForm'),taskInput=document.getElementById('taskInput'),priorityInput=document.getElementById('priorityInput'),taskList=document.getElementById('taskList'),totalTasksSpan=document.getElementById('totalTasks'),completedTasksSpan=document.getElementById('completedTasks'),filterBtns=document.querySelectorAll('.filter-btn'),progressBar=document.getElementById('progressBar'),progressPercentage=document.getElementById('progressPercentage'),progressLabel=document.getElementById('progressLabel'),progressTrack=document.querySelector('.progress-track'),streakDays=document.getElementById('streakDays'),weeklyAchievement=document.getElementById('weeklyAchievement');
let tasks=loadTasks(),completionDays=loadDays(),currentFilter='all',draggedId=null;
function loadTasks(){try{const saved=JSON.parse(localStorage.getItem('poketasks'));return Array.isArray(saved)?saved.map(t=>({...t,priority:t.priority||'medium'})):[]}catch{return[]}}function loadDays(){try{const saved=JSON.parse(localStorage.getItem('poketasks-completion-days'));return Array.isArray(saved)?saved:[]}catch{return[]}}function saveTasks(){localStorage.setItem('poketasks',JSON.stringify(tasks))}function saveDays(){localStorage.setItem('poketasks-completion-days',JSON.stringify(completionDays))}function dayKey(date=new Date()){return new Intl.DateTimeFormat('en-CA',{timeZone:'America/Guatemala'}).format(date)}
function playSound(type){try{const context=new (window.AudioContext||window.webkitAudioContext)(),oscillator=context.createOscillator(),gain=context.createGain(),now=context.currentTime;oscillator.type=type==='complete'?'square':'sine';oscillator.frequency.setValueAtTime(type==='complete'?523:210,now);oscillator.frequency.exponentialRampToValueAtTime(type==='complete'?880:110,now+.16);gain.gain.setValueAtTime(.045,now);gain.gain.exponentialRampToValueAtTime(.001,now+.19);oscillator.connect(gain).connect(context.destination);oscillator.start(now);oscillator.stop(now+.2)}catch{}}
function createShootingStars(){const c=document.getElementById('shootingStarsContainer');for(let i=0;c&&i<4;i++){const s=document.createElement('div');s.className='shooting-star';s.style.left=`${Math.random()*100}%`;s.style.top=`${Math.random()*40}%`;s.style.animationDuration=`${3+Math.random()*3}s`;s.style.animationDelay=`${Math.random()*7}s`;c.appendChild(s)}}function createParticles(){const c=document.getElementById('starsContainer'),colors=['#00f5ff','#ff00e6','#ffe600','#ff6b6b','#48dbfb'];for(let i=0;c&&i<45;i++){const p=document.createElement('div'),size=2+Math.random()*4;p.className='particle';p.style.width=p.style.height=`${size}px`;p.style.left=`${Math.random()*100}%`;p.style.background=colors[Math.floor(Math.random()*colors.length)];p.style.boxShadow=`0 0 ${size*2}px ${p.style.background}`;p.style.animationDuration=`${9+Math.random()*12}s`;p.style.animationDelay=`${Math.random()*10}s`;c.appendChild(p)}}
function visibleTasks(){return currentFilter==='pending'?tasks.filter(t=>!t.completed):currentFilter==='completed'?tasks.filter(t=>t.completed):tasks}function renderTasks(){taskList.replaceChildren();const shown=visibleTasks();if(!shown.length){const message=document.createElement('li');message.className='task-item empty-state';message.textContent=currentFilter==='all'?'No hay tareas... ¡agrega una!':currentFilter==='pending'?'¡Todas las tareas están completadas!':'No hay tareas completadas aún.';taskList.appendChild(message)}else shown.forEach(t=>taskList.appendChild(createTaskElement(t)));updateDashboard()}
function createTaskElement(task){const item=document.createElement('li'),handle=document.createElement('span'),checkbox=document.createElement('input'),text=document.createElement('span'),badge=document.createElement('span'),edit=document.createElement('button'),remove=document.createElement('button');item.className=`task-item${task.completed?' completed':''}`;item.draggable=true;item.dataset.id=task.id;item.addEventListener('dragstart',onDragStart);item.addEventListener('dragover',onDragOver);item.addEventListener('drop',onDrop);item.addEventListener('dragend',()=>item.classList.remove('dragging'));handle.className='drag-handle';handle.textContent='⋮⋮';handle.title='Arrastra para ordenar';checkbox.type='checkbox';checkbox.className='task-checkbox pokeball-checkbox';checkbox.checked=task.completed;checkbox.setAttribute('aria-label',`Marcar ${task.text} como completada`);checkbox.addEventListener('change',()=>toggleTask(task.id));text.className='task-text';text.textContent=task.text;badge.className=`priority priority-${task.priority}`;badge.textContent={high:'Alta',medium:'Media',low:'Baja'}[task.priority];edit.className='task-edit';edit.type='button';edit.textContent='✎';edit.title='Editar tarea';edit.addEventListener('click',()=>editTask(task.id));remove.className='task-delete';remove.type='button';remove.textContent='×';remove.title='Eliminar tarea';remove.addEventListener('click',()=>deleteTask(task.id,item));item.append(handle,checkbox,text,badge,edit,remove);return item}
function onDragStart(event){draggedId=event.currentTarget.dataset.id;event.currentTarget.classList.add('dragging');event.dataTransfer.effectAllowed='move'}function onDragOver(event){event.preventDefault();event.dataTransfer.dropEffect='move'}function onDrop(event){event.preventDefault();const targetId=event.currentTarget.dataset.id;if(!draggedId||draggedId===targetId)return;const from=tasks.findIndex(t=>t.id===draggedId),to=tasks.findIndex(t=>t.id===targetId);tasks.splice(to,0,tasks.splice(from,1)[0]);saveTasks();renderTasks()}
function updateDashboard(){const total=tasks.length,done=tasks.filter(t=>t.completed).length,percentage=total?Math.round(done/total*100):0;totalTasksSpan.textContent=`Total: ${total}`;completedTasksSpan.textContent=`Completadas: ${done}`;progressBar.style.width=`${percentage}%`;progressPercentage.textContent=`${percentage}%`;progressTrack.setAttribute('aria-valuenow',percentage);progressLabel.textContent=total?`${done} de ${total} tareas completadas.`:'Completa una tarea para comenzar.';const streak=getStreak();streakDays.textContent=streak;const weekDone=countWeekCompletions();weeklyAchievement.textContent=weekDone>=10?'¡Leyenda de la semana! Completaste 10 o más misiones.':weekDone>=5?'¡Gran entrenador! Ya llevas 5 misiones esta semana.':streak>=3?'¡Racha imparable! Sigue conquistando tus metas.':weekDone?`¡Buen inicio! Ya completaste ${weekDone} misión${weekDone===1?'':'es'} esta semana.`:'Tu próxima tarea puede iniciar una gran racha.'}
function countWeekCompletions(){const now=new Date(),start=new Date(now);start.setDate(now.getDate()-6);const keys=new Set;for(let i=0;i<7;i++){const d=new Date(start);d.setDate(start.getDate()+i);keys.add(dayKey(d))}return completionDays.filter(d=>keys.has(d)).length}function getStreak(){const dates=new Set(completionDays),today=new Date();let count=0;for(let i=0;i<365;i++){const d=new Date(today);d.setDate(today.getDate()-i);const key=dayKey(d);if(dates.has(key))count++;else if(i>0||!dates.has(dayKey(today)))break}return count}
function toggleTask(id){const task=tasks.find(t=>t.id===id);if(!task)return;task.completed=!task.completed;if(task.completed){const today=dayKey();if(!completionDays.includes(today)){completionDays.push(today);saveDays()}playSound('complete')}saveTasks();renderTasks()}function deleteTask(id,item){playSound('delete');item.style.cssText+=';opacity:0;transform:translateX(25px) scale(.96);transition:.3s';setTimeout(()=>{tasks=tasks.filter(t=>t.id!==id);saveTasks();renderTasks()},300)}function addTask(text){tasks.unshift({id:crypto.randomUUID?.()||`${Date.now()}-${Math.random()}`,text,priority:priorityInput.value,completed:false});saveTasks();renderTasks()}function editTask(id){const task=tasks.find(t=>t.id===id),next=prompt('Edita tu tarea:',task.text);if(next===null)return;const text=next.trim();if(!text)return;task.text=text;const priority=prompt('Prioridad: alta, media o baja',task.priority);if(['high','medium','low'].includes(priority?.toLowerCase()))task.priority=priority.toLowerCase();saveTasks();renderTasks()}function resetInput(){taskInput.removeAttribute('style');taskInput.placeholder='¿Qué tarea vas a capturar?'}function setFilter(filter){currentFilter=filter;filterBtns.forEach(b=>b.classList.toggle('active',b.dataset.filter===filter));renderTasks()}
taskForm.addEventListener('submit',event=>{event.preventDefault();const text=taskInput.value.trim();if(!text){taskInput.style.cssText='border-color:#ff5777;box-shadow:0 0 18px rgba(255,87,119,.4);animation:shake .3s ease';taskInput.placeholder='Escribe una tarea primero';taskInput.focus();return}addTask(text);taskInput.value='';resetInput();taskInput.focus()});taskInput.addEventListener('input',resetInput);filterBtns.forEach(b=>b.addEventListener('click',()=>setFilter(b.dataset.filter)));createShootingStars();createParticles();renderTasks();
